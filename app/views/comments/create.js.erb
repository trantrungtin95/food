<strong>Chat:</strong>
$('#new_comment').empty();
<% if @parent_comment.present? %>
    $('#children-comment-<%= @parent_comment.id %>').prepend('<%= j(render('comment', comment: @comment)) %>')
<% else %>
    $('#comment-list').prepend('<%= j(render('comment', comment: @comment)) %>')
<% end %>
